[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    needs_real_data: requires a completed pipeline run against the official extracts
filterwarnings =
    ignore::DeprecationWarning
